# Upgrade memof to Calcit 0.13.77 / 将 memof 升级到 Calcit 0.13.77

## 中文

- 将 `deps.cirru` 与 `@calcit/procs` 从 0.13.64 对齐到 Calcit 0.13.77，并刷新 Yarn lockfile。
- 将 `anchor-state` 的 attached example/test 与 Markdown 示例从 core Ref 解引用语法改为公开的 `.deref` trait 方法，以通过 0.13.77 的测试与文档类型检查。
- 保持 memof 0.0.29、Lilac 0.5.8、calcit-test 0.0.7、公开 API、缓存行为与既有类型债务 baseline 不变。

## English

- Align `deps.cirru` and `@calcit/procs` from 0.13.64 to Calcit 0.13.77 and refresh the Yarn lockfile.
- Update the `anchor-state` attached example/test and Markdown examples from core Ref dereference syntax to the public `.deref` trait method so they pass Calcit 0.13.77 test and documentation type checking.
- Keep memof 0.0.29, Lilac 0.5.8, calcit-test 0.0.7, public APIs, cache behavior, and the existing type-debt baseline unchanged.
