# 2026-08-22 00:00

- 将 memof 的 CI、脚本、README 和维护指引从旧 `cr` 命令迁移为 `calcit`。
- 将 workflow 从 `setup-cr` 更新到固定 commit 的 `setup-calcit`，并同步 Calcit 0.13.30、Lilac 0.5.3 与 `@calcit/procs`。
- 保持 canonical `calcit.cirru`，不再引入或引用 `compact.cirru`。
