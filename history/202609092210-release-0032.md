# Release 0.0.32

Calcit 0.14.5 compatibility and stricter type checking.

- Added typed cache boundaries for `memof1-call` and `memof1-call-by`.
- Added `clear-memof1-function!` for clearing one function identity without exposing cache internals.
- Enabled native quality, public API, example, and dynamic dispatch gates.
- Kept only the reviewed open-state contracts required by the generic memoization APIs.
